<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Warna saat tombol di-check (aktif) -->
    <item android:color="@color/app_pink_primary" android:state_checked="true" />

    <!-- Warna default (tidak aktif) -->
    <item android:color="@color/app_text_dark" android:state_checked="false" />

</selector>